export function epikjsr(bad: boolean) {
    if(bad == true) {
        console.error("MY BRLALS")
    } else {
        console.log("The most epik package of them all")
    }
}

// Totally isnt taken right from epikcoolpackage on pub
export function adder(epiknumber: number) : number {
    const resultargh = epiknumber + 3

    return resultargh
}